# betwixt-and-between — Build TODOs

Generated by /plan-eng-review on 2026-04-22; reorganized 2026-05-14.
For full per-version detail see `CHANGELOG.md`. For design specs see
`~/.gstack/projects/jzeng151-betwixt-and-between/` and `~/.gstack/projects/betwixt-and-between/`.

---

## Shipped

### v0.0.x – v0.1.0 — Foundation
- Drizzle schema (`entities`, `relationships`, `canvas_positions`), strict TS, FK + cascade tests.
- API endpoints for entities, relationships, canvas positions.
- Stores + `openEntity`/`windowStore` cross-window navigation; per-entity windowed instances; taskbar grouping.
- Window manager: drag/resize/minimize, cascading spawn, `Ctrl+W`/`Ctrl+Tab`/`Esc`/`Tab`/`Enter`/`Ctrl+K` shortcuts, focus rings, < 1280 px lockout, first-open tutorial.
- Story Graph (tldraw), Character/Wiki/Timeline editors, World Map (Phase 1, Leaflet + polygon regions), Midnight Ink design tokens.

### v0.1.1 – v0.1.2 — QoL + observability
- Palette search, collapsible palette + Characters section, spotlight position label, Cancel button in editors, bar tooltip portal.
- Sentry client + server, `adapter-node` pin.

### v0.1.3 – v0.1.5 — Preferences scaffold + Wiki rework
- `src/lib/stores/preferences.ts` foundation (localStorage-backed, schema-version migrations, defensive load path) — see `T9-prefs` below for follow-on.
- Per-domain `src/lib/types/` submodule.
- Wiki rebuilt as alphabetical entity browser with `[[Name]]` cross-links, right-click "Open focused graph"/"Open focused timeline", scope-dim on playhead.
- Notes-as-sections via `note_of` relationship; CharacterEditorBody + EntityDetail body-field unification; in-window `wiki-nav`; link-preview pane + Editor settings toggle.

### v0.1.0 – Phase 1B — Architecture rework
- **PR 1** Intervals schema + migration + `writeInterval` chokepoint + `recomputeAllIntervals`/`ForAct` + compat layer + invariant tests + Delete-Act cascade.
- **PR 2** V2 timeline UI (became THE timeline 2026-04-28). `IntervalBar` width breakpoints, `data.timelineLabel`, palette drag, Alt-snap, multi-act spans, break-into-scenes, V1 retired in single hard cutover.
- **Phase 1.5** Scrubber playhead store, `PlayheadOverlay`, Story Graph + World Map dim-on-scope subscribers.
- **Phase 1.6** Editor + structural moves: `ActEditor`/`EventEditor`/`SceneEditor`, `EntityDetail` host-agnostic router, side-panel + pop-out windows, hover-`+`/grip drag-reorder, delete-with-reparent, `splitInterval`/`moveSceneToAct`/batch endpoint, `relationships_dedup` migration, draft-preserving Toast.
- **Phase 1B graph** Lane Z cleanup (drop tldraw/react, `<GraphCanvas>` extraction). Lane A `window_canvas_state` + per-window endpoints. Lane B `edge-policy`, traversal helpers, `<ContextMenu>`. Lane C `<FocusedGraph>` + Legend + dagre layout + `<TypeOrderPanel>` + multi-rel fan-out + StoryGraph hard-filter.
- **T5 Bar translation** — pointer-down on bar body translates with 4 px threshold, scrub-mode exclusive.
- **T6 Wiki rework** — completed alongside the v0.1.5/v0.2.0 wiki ship; routed Character/Location/Note through `EntityDetail`.
- **T-QOL-TESTS** — five Playwright gaps from v0.1.1 closed.

### v0.2.0 — World Map (Phase 1) + EntityDetail expansion
- Leaflet bitmap import, polygon regions with self-intersection rejection, scope-driven glow/dim, multi-map switcher, region→Location linking, R2-staged uploads (later moved to R2 in v0.4.0.0).
- Pending-commit registry, in-window chip nav, link-preview pane.

### v0.3.0.0 — Scroll Theatre landing page
- `/` landing with sticky MiniDesktop and 4 alternating scroll sections; desktop app moved to `/app`.

### v0.4.0.0 — Multi-user + Cloudflare deploy
- **T8a** sqlite→Postgres port (PR #8): `pgTable`, `jsonb`, native `uuid().defaultRandom()`, BEFORE-UPDATE triggers, regenerated migrations, PGlite test helper. Polymorphic-tx work folded in (closed deferred T1 + D17).
- **T8b** Better-Auth + multi-user + Cloudflare: per-request `buildAuth(db, env)`, hook-owned db lifecycle, 22 routes gated by `getUserId`, `intervals.ts` userId-threaded across 20 functions, 36 isolation tests, magic-link via Resend, Google OAuth, weekly B2 backup workflow, DEPLOY.md.

### v0.5.0.0 — Story Player ("Spotlight")
- Standalone floating window with compact transport bar (play/pause/step/scrub/speed), pin-to-top, taskbar grouping with Timeline, `compact` + `alwaysOnTop` window infrastructure, 4 Playwright specs.

### v0.5.0.1 — story-structure helper extraction
- Shared `getActs`/`getScenesByActId`/`getSceneBoundaries`/`getSpotlightLabel`/`stepForwardScene`/`stepBackScene` so Timeline + PlayerDock stop carrying duplicate copies.

### v0.5.0.2 — feat/app-qol
- Shared `DeleteConfirmDialog`, per-entity-type colors via `--color-type-*` tokens + `entity-type-colors.ts`, Leaflet `imageOverlay`/region `$state` reactivity fixes, R2 `Content-Type`/`Cache-Control` correctness, edge dimming via `opacity` not `stroke-opacity`, Leaflet-Draw edit/delete buttons removed (broken; reshape returns in WorldMap v2).

### v0.6.0.0 — World Map v2 groundwork + Step 1
- `world_maps.location_id` + `location_inactive_at` + `assertLocationIdIsLocation` + Vitest invariant test.
- Linked-location picker on the WorldMap toolbar.
- "Maps" section in Location editor (create-map / open-maps CTAs).
- "Open map" context-menu action in StoryGraph + FocusedGraph for Location nodes.
- Deep-link `entityId` effect prefers explicit `location_id` over region-link match.

### Spotlight visualization layer (Phase 1.5 follow-ons)
- **T11-mystery** — `Relationship.revealedAtPosition`, `updateRelationship`, edge right-click in `GraphCanvas`, `mysteryMode` rendering with `--color-rel-mystery`, `▼ Advanced` disclosure in StoryGraph create form, `EditRelationshipModal`.
- **T10-ghost** — `GraphEdge.ghostMode` past/future, ±2-act window, dasharray + opacity per past/future, mystery > ghost precedence honored.
- **T12-alias** — `entity_aliases` table + store, alias-edge emission with `"aka"` label, dashed-border on aliased nodes, `AliasModal` from node right-click, Legend `"aka"` row.
- **T13-playback** — `playhead.ts` `play`/`pause`/`stepForward`/`stepBack`/`jumpTo` + `isPlaying`/`playbackSpeed`. Standalone Story Player (v0.5.0.0) is the surfaced UI.
- **Wiki sidebar scope-dim subscriber** — landed alongside Wiki rework (v0.1.5).

---

## In flight

### World Map v2 — Story-Time Engine on Spatial Surfaces

Branch `feat/world-map-v2-groundwork`, PR #42. Design doc:
`~/.gstack/projects/betwixt-and-between/steve-feat-app-qol-design-20260513-175833.md`
(includes Plan-Eng-Review Addendum v2.1 with locks M1–M11).

7-step build order, each shippable on its own:

- [x] **Step 1 — Map-Location linking UI** (v0.6.0.0). Picker in WorldMap settings + "Open this Location's map" entry points.
- [ ] **Step 2 — Drill-down navigation.** Click polygon whose linked Location has a child Location with a map → open child map. Breadcrumb up. "Create a map for X?" offer when child has no map.
- [ ] **Step 3 — Map variants.** Scene-range picker on map settings; variant resolution at playback (find variant whose range covers playhead; fall back to default); editor enforces non-overlapping at save (M9 EXCLUDE constraint); duplicate-map action clones variant with regions.
- [ ] **Step 4 — `Artifact` entity-type.** Palette chip, drag-onto-map placement (M1 `map_placements` first-class table), intervals binding via scene-range picker, `targetEntityId` picker, Leaflet marker with target's icon (overridable per Artifact).
- [ ] **Step 5 — Movement playback.** Two adjacent Artifacts with same `targetEntityId` + same `mapId` + abutting intervals → tween marker position between them as playhead crosses boundary. Optional trail polyline.
- [ ] **Step 6 — `EventChain` entity-type.** Ordered artifact picker, payload composer, scope selector (`active-scene` / `persistent` / `within-N-seconds`). Playback runtime tracks per-chain progress; persistent scope saves to `event_chain_progress` (M7); CAS update for safety.
- [ ] **Step 7 — Spotlight map cycling.** As playhead advances, find most-specific active Location with a map and animate WorldMap to it (fade between images; pan/zoom to fit new variant's bounds).

---

## Open / pending

### Wiki / Editor polish (unblocked — wiki-rework shipped v0.2.0)

- [ ] **T9-uuid — `[[#uuid]]` ID-fallback link syntax.** Escape hatch for duplicate-name disambiguation. Today: warn-on-ambiguity, first-match wins. ~30 lines parser change.

- [ ] **T13-aliassyntax — `[[Entity Name|alias]]` markdown alias syntax.** Obsidian/MediaWiki convention. `[[Aragorn|the king]]` renders "the king", resolves Aragorn.

### Timeline polish

- [ ] **T2 — Window snapping in `Window.svelte`.** Drag-to-edge zones (halves, quarters). Pure UX additive on `windowStore.move`/`resize`. No data-model change.

- [ ] **T4 — Status field for Acts/Events/Scenes.** `Drafted` / `In progress` / `Done` / `Needs revision`. Top-of-panel pill in editors; optional timeline filter. Stored in `entity.data` (no schema migration).

- [ ] **T7 — Bulk paste / import UI.** Multi-entity import from external story-craft tools (Plottr CSV, Aeon export). Uses `/api/entities/batch`. Drag-drop file or paste structured text → preview → commit.

### Preferences

- [ ] **T9-prefs — Promote second client preference into the prefs store.** `src/lib/stores/preferences.ts` foundation shipped v0.1.3 (localStorage, schema-version migrations, defensive load). Per the original T9 spec, items to home here once they exist as second/third entries: hard/soft edge filter toggle, scrubber label format (decimal vs act-name), ghost trail opacity. **Action:** audit current preferences keys vs the planned list; promote anything still component-local; close out T9 once the store has the spotlight settings on it.

### Search

- [ ] **T16 — Cmd-K full-body search index.** Extend the (yet-to-ship) Cmd-K Fuse.js index to include `entities.data.body_md`. Trade-off: ~10× index size growth (10k × 1KB ≈ 10MB), first-keypress latency. **Depends on:** Cmd-K shipping. Defer body-content path until names+types+note-titles index ships and feedback shows it's not enough.

### Multi-user / deploy follow-ups

- [ ] **T12-encrypt — pgcrypto column-level encryption for sensitive entity bodies.** `pgp_sym_encrypt` on `entities.data` body fields (`body_md` for Note). User-derived key from password+salt. Search-by-name stays plaintext (Wiki sidebar still works). End-to-end browser encryption rejected (breaks Cmd-K and future collab). Why deferred: ship α first; add only if user feedback shows it matters.

- [ ] **T14 — Endpoint registry route-tree generator.** Auto-generate `/api/**` registry from SvelteKit `+server.ts` files at test-collection time. Replaces hand-maintained list which rots when new endpoints don't get added. Used by user_id isolation parametric sweep. ~50 LOC test-time codegen.

- [ ] **T15 — `app_locked` maintenance-mode flag.** If a future T8b-like backfill needs to re-run after launch (schema migration touching userId), gate the migration window. Login flow shows "Maintenance, back in a few." Why deferred: T8b's first run was pre-launch; only needed once scaling.

### World Map fog-of-war (post-v2)
The current World Map v2 work (Steps 1–7 above) doesn't touch hex grid / fog-of-war.
The items below are queued behind that future feature and only become actionable
after a hex-fog branch starts.

- [ ] **Hex grid overlay + fog-of-war** — original Phase 4 World Map upgrade. Scope a separate design pass before scheduling.

- [ ] **T10-hexlock — Postgres BEFORE UPDATE trigger for `world_maps.hex_size` immutability.** Belt-and-suspenders for the hex-fog feature. App-layer (`PATCH /api/maps/[id]`) is primary; trigger backs direct SQL writes. Raises if `hex_size` changes when reveal rows exist.

- [ ] **T11-hexrecompute — Spatial projection of `hex_size` changes.** Recompute `map_hex_reveals` and `map_hex_region_overrides` when user changes `hex_size` on a map with existing overrides. Project (x, y) coords old → new grid, accept rounding loss, surface preview + confirm. Why deferred: rare path, error-prone math, lots of UX surface. Depends on T10-hexlock.

### Generic icon registry — vocabulary expansion

- [ ] **Extend icon vocabularies beyond Character.** `src/lib/icons/registry.ts` ships character archetypes only (Heroes/Villains/Mystics/Wanderers, ~13 Lucide). Storage shape `entity.data.icon = "lucide:<name>"` is library-prefixed for easy swap. Vocabularies still missing:
  - **Locations** — castle/forest/cave/city/ruins/harbor/temple. Lucide is thin; consider Game-icons subset (CC-BY 3.0, requires attribution).
  - **Events** — crossed-swords/scroll/heartbreak/coronation/explosion. Same gap as Locations.
  - **Scenes** — likely inherits from Event vocabulary.
  - **Notes** — existing Lucide pin/bookmark/document fits.
  - **Refactor first:** extract `.icon-picker` markup + CSS from `CharacterEditor.svelte` to `src/lib/components/IconPicker.svelte` before the second entity type adopts it.

---

## Future feature: temporal identity & relationships

Surfaced 2026-05-01 while seeding *The Prestige* (`scripts/seed/prestige.ts`). The current data model is **static** — relationships exist or don't — but stories have identities and allegiances that change across story-time. T11-mystery + T12-alias (now shipped) covered the partial first cut (reveal gating + entity_aliases as a separate table). Full temporal-validity story still pending:

- **Temporal validity on relationships** — optional `start_act_id`/`end_act_id` (or position columns once intervals' positioning is generalized) on relationship rows. NULL = "always true."
- **Spotlight integration** — scrubber at story-time T hides relationships whose interval doesn't contain T. Subsumes the current static-graph view as the always-true special case.
- **Reveal gating** — covered by shipped T11-mystery.
- **Identity aliases** — covered by shipped T12-alias (separate table, locked over an `aka` relationship type).

Big feature. Re-visit when the seeded *Prestige* dataset surfaces concrete UX pain that the static graph can't represent.

---

## Purged from prior version of this file

For posterity (so old PR/commit references resolve):
- **T1 (Drizzle sync-mode refactor)** — superseded by T8a; Postgres port supports `db.transaction(async tx => ...)` natively. Branch `refactor/intervals-sync-t1` repurposed.
- **Phase 1–5 verbose checklists** — collapsed to one-liners under `## Shipped`. Per-feature detail lives in `CHANGELOG.md` and the design docs under `~/.gstack/projects/`.
- **Midnight Ink Palette CSS block** — implementation lives in `src/app.css` + `src/lib/styles/tokens.css`. Don't edit it here.
- **"Interaction states" empty/loading/error table** — current behavior is in the relevant components (`Timeline.svelte`, `StoryGraph.svelte`, etc.). Was a planning artifact, not a spec.
- **"NOT in scope (v1)"** — most items shipped (multi-user via T8b, scrubber via Phase 1.5) or were re-scoped. Remaining genuinely-out-of-scope items (game-engine export, taskbar clock widget) aren't load-bearing for any current decision.
