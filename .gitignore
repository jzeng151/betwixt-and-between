node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
.dev.vars
!.dev.vars.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# SQLite
*.db

# gstack tooling
.gstack/

# Generated vitest examples
src/lib/vitest-examples/
test-results/
.claude/
desync-bug.png
tooltip-bug.png
test.db
.gitnexus

# User-uploaded map images (R2 in prod, local fs in dev)
static/maps/

# Engineering docs — generated + locally authored, not tracked.
# Regenerate the generated ones with `npm run docs:all`.
# Anchored to root so scripts/docs/ (the generators) is unaffected.
/docs/
